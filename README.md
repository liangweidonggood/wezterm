# Wezterm 配置

基于 [KevinSilvester/wezterm-config](https://github.com/KevinSilvester/wezterm-config) 二次开发的个人 [Wezterm](https://wezfurlong.org/wezterm/) 终端配置，采用模块化结构，带有美观的背景图片和完整的快捷键支持。

## 特性

- 🎨 **模块化设计** - 按功能拆分配置文件，易于维护
- 🖼️ **随机背景** - 支持多张背景图片随机切换
- ⌨️ **自定义快捷键** - 完整的vim风格按键绑定
- 🌍 **跨平台支持** - 自动适配 macOS / Windows / Linux 不同平台
- ✨ **GPU加速** - 启用GPU渲染，流畅丝滑
- 🐧 **默认WSL** - Windows 下默认直接启动 WSL: kali-linux (root/zsh)

## 致谢

原始配置框架来自：https://github.com/KevinSilvester/wezterm-config

## 修饰键说明

所有快捷键使用修饰键缩写：

| 操作系统       | `SUPER` | `SUPER_REV` | `LEADER`          |
|----------------|---------|-------------|-------------------|
| macOS          | `Cmd`   | `Cmd+Ctrl`  | `Cmd+Ctrl+Space`  |
| Windows/Linux  | `Alt`   | `Alt+Ctrl`  | `Alt+Ctrl+Space`  |

## 快捷键参考

### 杂项

| 快捷键                | 功能                     |
|-----------------------|--------------------------|
| `F1`                  | 激活复制模式             |
| `F2`                  | 打开命令面板             |
| `F3`                  | 显示启动器               |
| `F4`                  | 显示启动器（仅标签页）   |
| `F5`                  | 显示启动器（仅工作区）   |
| `F11`                 | 切换全屏                 |
| `F12`                 | 显示调试覆盖层           |
| `SUPER + f`           | 搜索终端文本             |
| `SUPER_REV + u`       | 打开光标下的URL          |

### 复制粘贴

| 快捷键                | 功能                     |
|-----------------------|--------------------------|
| `Ctrl + Shift + c`    | 复制到剪贴板             |
| `Ctrl + Shift + v`    | 从剪贴板粘贴             |
| `Shift + Insert`      | 从剪贴板粘贴 *(新增)*    |
| `Shift + Enter`       | 在 shell 中插入换行 *(新增)* |

### 光标移动

| 快捷键                | 功能                     |
|-----------------------|--------------------------|
| `SUPER + LeftArrow`   | 跳转到行首               |
| `SUPER + RightArrow`  | 跳转到行尾               |
| `SUPER + Backspace`   | 清空整行                 |

### 标签页

#### 新建/关闭

| 快捷键                | 功能                     |
|-----------------------|--------------------------|
| `SUPER + t`           | 新建标签页（默认域）     |
| `SUPER_REV + t`       | 新建标签页（WSL:Ubuntu）|
| `SUPER_REV + w`       | 关闭当前标签页           |

#### 导航

| 快捷键                | 功能                     |
|-----------------------|--------------------------|
| `SUPER + [`           | 切换到上一个标签页       |
| `SUPER + ]`           | 切换到下一个标签页       |
| `SUPER_REV + [`       | 标签向左移动             |
| `SUPER_REV + ]`       | 标签向右移动             |

#### 切换/编辑

| 快捷键                | 功能                     |
|-----------------------|--------------------------|
| `SUPER + 9`           | 切换标签栏可见性         |
| `SUPER + 0`           | 重命名当前标签页         |
| `SUPER_REV + 0`       | 重置标签页名称           |

### 窗口

| 快捷键                | 功能                     |
|-----------------------|--------------------------|
| `SUPER + n`           | 新建 Wezterm 窗口       |
| `SUPER + -`           | 缩小窗口尺寸             |
| `SUPER + =`           | 增大窗口尺寸             |
| `SUPER_REV + Enter`   | 最大化窗口               |

### 窗格

#### 分割窗格

| 快捷键                | 功能                     |
|-----------------------|--------------------------|
| `SUPER + \`           | 垂直分割窗格             |
| `SUPER_REV + \`       | 水平分割窗格             |

#### 缩放/关闭

| 快捷键                | 功能                     |
|-----------------------|--------------------------|
| `SUPER + Enter`       | 切换窗格缩放             |
| `SUPER + w`           | 关闭当前窗格             |

#### 导航

| 快捷键                | 功能                     |
|-----------------------|--------------------------|
| `SUPER_REV + k`       | 焦点移到上方窗格         |
| `SUPER_REV + j`       | 焦点移到下方窗格         |
| `SUPER_REV + h`       | 焦点移到左侧窗格         |
| `SUPER_REV + l`       | 焦点移到右侧窗格         |
| `SUPER_REV + p`       | 选择并交换窗格           |

#### 滚动

| 快捷键                | 功能                     |
|-----------------------|--------------------------|
| `SUPER + u`           | 向上滚动 5 行            |
| `SUPER + d`           | 向下滚动 5 行            |
| `PageUp`              | 向上滚动一页             |
| `PageDown`            | 向下滚动一页             |

### 背景图片

| 快捷键                | 功能                     |
|-----------------------|--------------------------|
| `SUPER + /`           | 随机加载背景图片         |
| `SUPER + ,`           | 切换到上一张背景         |
| `SUPER + .`           | 切换到下一张背景         |
| `SUPER_REV + /`       | 模糊搜索选择背景图片     |
| `SUPER + b`           | 切换背景焦点效果         |

### 按键表

这些快捷键会激活临时按键表：

1.  **调整字体大小**: `LEADER + f`

    | 按键  | 功能                 |
    |-------|----------------------|
    | `k`   | 增大字体             |
    | `j`   | 减小字体             |
    | `r`   | 重置为默认字体大小   |
    | `q` / `Esc` | 退出调整模式 |

2.  **调整窗格大小**: `LEADER + p`

    | 按键  | 功能                 |
    |-------|----------------------|
    | `k`   | 增加高度             |
    | `j`   | 减少高度             |
    | `h`   | 减少宽度             |
    | `l`   | 增加宽度             |
    | `q` / `Esc` | 退出调整模式 |

## 安装

```bash
# 备份原有配置
mv ~/.config/wezterm ~/.config/wezterm.bak

# 克隆本仓库
git clone https://github.com/liangweidonggood/wezterm.git ~/.config/wezterm

# 重启 Wezterm
```

## 目录结构

```
.
├── backdrops/          # 背景图片
├── colors/             # 颜色配置
├── config/             # 配置模块
│   ├── appearance.lua  # 外观配置
│   ├── bindings.lua    # 按键绑定
│   ├── domains.lua     # 域配置(WSL等)
│   ├── fonts.lua       # 字体配置
│   ├── general.lua     # 通用配置
│   ├── init.lua        # 配置入口
│   └── launch.lua      # 启动配置
├── events/             # 事件回调
├── utils/              # 工具函数
├── CLAUDE.md           # Claude Code 项目约定
├── LICENSE             # MIT 许可证
├── README.md           # 项目说明
└── wezterm.lua         # 主入口
```

## 依赖

- [Wezterm](https://wezfurlong.org/wezterm/) >= 20240203

## 许可证

MIT License
